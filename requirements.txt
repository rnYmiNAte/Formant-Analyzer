# Core runtime dependencies
numpy>=1.24.0,<2.0.0              # Numerical arrays & math
scipy>=1.10.0                      # Signal processing helpers (resample, etc.)
matplotlib>=3.7.0                  # Plotting the vowel trapezoid
soundfile>=0.12.0                  # Reliable WAV file reading (better than wave module)
librosa>=0.10.2                    # LPC implementation (Burg's method) - critical for formant extraction

# Optional but recommended for better CLI experience
tqdm>=4.66.0                       # Progress bars if you later add batch processing

# Development & testing dependencies (install with pip install -r requirements.txt[dev])
pytest>=7.4.0                      # Unit testing framework
pytest-cov>=5.0.0                  # Coverage reports (optional but useful)
black>=24.0.0                      # Code formatter
ruff>=0.5.0                        # Fast linter & formatter (alternative to flake8 + isort)
mypy>=1.10.0                       # Static type checker

# If you want to generate documentation later
mkdocs>=1.5.0                      # Optional: for nice project docs
mkdocs-material>=9.5.0             # Material theme for mkdocs
